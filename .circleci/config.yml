# This config was automatically generated from your source code
# Stacks detected: cicd:jenkins:python-jenkins-argocd-k8s,deps:java:java-maven-sonar-argocd-helm-k8s/spring-boot-app,deps:python:python-jenkins-argocd-k8s,file:manage.py:python-jenkins-argocd-k8s
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-java:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project/java-maven-sonar-argocd-helm-k8s/spring-boot-app
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Calculate cache key
          command: |-
            find . -name 'pom.xml' -o -name 'gradlew*' -o -name '*.gradle*' | \
                    sort | xargs cat > /tmp/CIRCLECI_CACHE_KEY
      - restore_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
      - run:
          command: mvn verify
      - store_test_results:
          path: target/surefire-reports
      - save_cache:
          key: cache-{{ checksum "/tmp/CIRCLECI_CACHE_KEY" }}
          paths:
            - ~/.m2/repository
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.8-node
    working_directory: ~/project/python-jenkins-argocd-k8s
    steps:
      - checkout:
          path: ~/project
      - python/install-packages
      - run:
          name: Run tests
          command: python manage.py test
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: found jenkins config
          command: ':'
workflows:
  build-and-test:
    jobs:
      - test-java
      - test-python
    # - deploy:
    #     requires:
    #       - test-java
    #       - test-python
